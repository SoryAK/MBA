/**
 * CLI names for daemon HTTP JSON. The shapes live in `service/contracts.ts`.
 */

export type {
  BootResult,
  ModelConfig,
  ModelHistory,
  ModelHistoryEvent,
  ModelShelfCard,
  ModelWatches,
  PairingSession,
  ServerEntry,
  SetResult,
  WatchRow,
} from "../service/contracts.js";
